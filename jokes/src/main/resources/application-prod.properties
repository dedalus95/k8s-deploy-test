#set datasource.url to environment variable
spring.datasource.url=${DATASOURCE_URL}
#set datasource.username to environment variable
spring.datasource.username=${DATASOURCE_USERNAME}
#set datasource.password to environment variable
spring.datasource.password=${DATASOURCE_PASSWORD}
